type: dbt_mesh_demo.components.dbt_mesh_component.DbtMeshComponent

attributes:
  project: "{{ context.project_root }}/dbt_projects/silver_project"
  auto_partition_microbatch: true
  cli_args:
    - build
    - "--vars":
        min_date: "{{ partition_key }}"
  group_overrides:
    seed: raw
    staging: bronze
    silver: silver
